# 조합적 문제
- 부분 집합
- 조합
# 탐욕알고리즘
- 그리디
- Snapsack 문제
- 활동 선택 문제
- 연습 문제

## 부분집합
어떤 집합의 공집합과 자기자신을 포함한 모든부분
1. 완전탐색(학습용)



2. binary counting(실전용)
    - 0에서 부분집합의 수 만큼 반복코드
    - 이진수와 비교

## 순열과 조합 차이

## Greed 탐욕 알고리즘
대표적인 문제해결 기법
1. 완전탐색 -> 시간 메모리 초과하면 밑에꺼 떠올리기
2. Greedy : 결정이 필요할떄 가장 좋아보이는 선택지로 선택
   - 도대체 어떤 문제가 그리디인가?
   - 규칙성을 찾아야 한다.
     - 규칙을 못 찾으면 못푼다.
     - 규칙을찾았다!(최적의규칙)
     - 그리디로 풀 수 있는지 검증
     - 1. 탐욕적 선택 조건(Greedy Choice Property)
       - 각 단계의 최적해 선택이 이후 단계 선택에 영향을 주지 않는다.
       - 즉, 각 단계 규칙이 변경되면 안된다.
       - 동전 문제
         - 첫 번째 단계: 가장 큰 동전(500원)으로 가능한 만큼 준다.
         - 두 번째 단계: 가장 큰 동전(100원)으로 가능한 만큼 준다.
         - ...
         - > 각 단계의 규칙이 유지되고, 이전에 구했던 최적해 선택이 각 단계에 영향X
     - 2. 최적 부분 구조(Optimal Substructure)
       - 각 단계의 최적해 선택을 합하면, 전체 문제의 최적해가 되어야 한다.
         - 증명을 통해 해결
       - 동전 문제 예시
         - [명제] 가장 큰 동전부터 순서대로 고르면 최소 동전 수가 나온다
         - [간접증명]
           - 최적해보다 더 작은 수의 동전으로 표현 가능하다.(가정)
           - > N원을 더 작은 수의 동전으로 표현할 수 있다.
             - => 동전이 배수로 있기 때문에 숫자가 더 작으면 무조건 더 큰 수의 동전
             - => 수학적으로 말이 안된다( 더 작은 수로 나누었는데 몫이 작다)
             - => 모순 발생 (원레 명제가 참)
     - 3. 반례가 없는가?
3. DP : 


### sort 사용법
sort(정렬기준)
sort(key=lambda x:x[0])

### 화장실 문제
### Knapsack문제
### 활동선택문제

# 정리
- 완전 탐색
  - 모든 경우의 수를 보자
  - 부분집합,순열,조합을 배웠다.

- 그리디
  - 규칙을 적용할 수 있는지 검증
  - 입력에 따라 나올 수 있는 여러가지 케이스를 보고 규칙을 정해보자!
  - > 결국 그리디의 정답은 많은 케이스를 보는 것