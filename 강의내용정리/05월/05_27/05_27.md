# Template Syntax
1. Text Interpolation

# Directive
- 'v-' 접두사가 있는 특수 속성
- v-on:submit.prevent="value"

# v-bind
- 태그의 속성을 반응형으로 변경
1. Attribute Bindings
    - v-bind:속성명="ref객체"
    - v-bing를 생략하여, :으로 시작가능.
2. Class and Style Bindings

# Class(속성) and Style(속성) Bindings
- 클래스를 동적으로 전환
    - :class="{active: isActive}
    - isActive가 true이면 active가 할당