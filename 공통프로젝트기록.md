# 그냥 명세서 보고 공부하기
- AI 이후의 역할분담
    1. AI 프론트엔드 개발자(AI상호작용UI+기존UX+UI)
    2. AI 백에드 개발자(AI파이프라인(추론,벡터검색,RAG등)통합 + 기존 백엔드)
    3. 데이터 엔지니어(AI 데이터 파이프라인(대용량 학습 데이터, 벡터 DB 및 임베딩 관리 등) + 기존DB)
    4. MLOps(모델 생명 주기 관리 + 기존 DevOPS)
    5. AI/ML 엔지니어(도메인 별 모델 리서치, 모델 성능 튜닝 및 평가)
    6. 프롬프트 엔지니어(시스템 프롬프트 체인 설계)
- AI 아카이브 시스템 개요
    - 사용자가 스캔한 문서,사진,메모를 자동 분류하고 요약하여 검색이 가능한 디지털 아카이브로 변환하는 문서 관리 시스템
    1. 시나리오
        - 1단계: 영수증 사진 촬영 -> DiT가 문서 유형 자동 분류 -> 영수증 카테고리 자동 할당
        - 2단계: 이미지 업로드 완료 -> PaddleOCR + Donut 이 텍스트 추출 -> 스타벅스 아메리카노 4500원 인식
        - 3단계: 정보 추출 요청 -> LayoutLMv3 가 구조화된 정보 추출 -> 날짜,금액,상홍 자동 파싱
        - 4단계: 자동 요약 진행 -> KoBART가 핵심 내용 요약 -> 2024.1.15 스타벅스 음료 구매
        - 5단계: "커피 영수증" 검색 -> 벡터 유사도 또는 키워드 매칭 -> 관련 영수증 목록 즉시 제공
    2. AI 기술 조합
        - AI기술 -> AI기능 -> 역할 -> 사용모델
        - 컴퓨터비전 -> 이미지기반분류 -> 문서유형자동인식 -> DiT(Document Image Transformer)
        - 컴퓨터비전 -> OCR -> 정확한 문자 인식 -> PaddleOCR + Donut (영수증 특화)
        - 컴퓨터비전 -> 문서인식 -> 문서 데이터 추출 -> LayoutLMv3
        - 자연어처리 -> 텍스트요약 -> 핵심 내용 압축 -> KoBART
    3. 그 외 기술 스택
        - SQLite + SQLModel => 문서 정보 저장 및 검색
        - Streamlit => 사용자 인터페이스
    4. 비고
        - 난이도: 초급
        - 주요 챌린지: OCR 품질확보, 문서 유형 탐지 및 추출 정보 정확도, 검색 성능
        - 연관 산업: 기업 문서 관리 솔루션, 개인 생산성 도구, 금융/법률/의료의 문서관리
- AI 아카이브 시스템
    - 컴퓨터 비전, 자연어 처리를 통한 문서 아카이브 어플리케이션을 개발해보겠습니다. 사용자가 문서 이미지를 업로드 하면 AI가 문서의 유형을 분류하고, 문서의 요약 내용, 키워드, 구조화 된 정보들을 추출하여 저장합니다. 그리고 사용자는 저장도니 문서를 베거 유사도로 검새갛거나 키워드로 검색할 수 잇습니다.
    5. 프로젝트 기술 스택
        1. Streamlit: Streamlit은 Python 기반 오픈소스 웹 어플리케이션 프레임워크로 데이터 과학자와 AI 개발자를 위해 설계되었습니다. Tornado 웹 서버와 React 기반의 프론트에드로 구성 되어있으며 Hot-reloading 기능을 지원하는 등 Python 기반으로 web UI 를 쉽고 빠르게 개발하게 해줍니다.
        2. DiT(Document Image Transformer): DiT는 Microsoft에서 개발한 문서 이미지 분류 모델로 Vision Transformer(ViT) 아키텍처를 기반으로 400,000 개의 문서 이미지, 16개의 카테고리로 사전 학습되었습니다. 이미지를 16x16 으로 분할하여 토큰으로 전환하고 문서의 전역적 특징을 학습합니다.
        3. PaddleOCR: PaddleOCR은 바이두에서 개발한 오픈소스 OCR 엔진으로, 텍스트 검출, 방향 분류, 텍스트 인식의 3단계 파이프라인을 구현합니다.한국어 포함 80개 이상의 언어를 지원합니다.
        4. LayoutLMv3: LayoutLMv3는 Microsoft에서 개발한 멀티모달 문서 인식 모델로 텍스트, 레이아웃(위치정보),이미지를 통합하여 처리합니다. 이 프로젝트에서는 OCR 처리 후 추출된 텍스트와 바운딩 박스 정보를 활용하여 문서의 유형(문서,영수증0에 따른 문서의 구조적 정보(제목,본문,날짜,금액 등)를 추출하는데 사용됩니다.)
        5. Donut(Document Understanding Transformer): Donut은 네이버 클로바에서 개발한 OCR-free 문서 인식 모델로 별도의 OCR 과정 없이 이미지에서 직접 구조화된 정보를 추출합니다. 이 프로젝트에서 쓰이는 모델은 영수증 데이터셋으로 Fine-tuning 되었으며 영문 모델로 LayoutLMv3를 보완하는 용도로만 쓰입니다.
        6. Ko-SRoBERTa: KO-SRoBERT 는 문장을 768 차원의 밀집 벡터로 인코딩하여 의미적으로 유사한 문장은 코사인 유사도가 높도록 학습된 문장 임베딩 모델입니다. 이 프로젝트에서 문서의 요약과 키워드를 통해 유사한 의미를 가진 문서들을 검색하는데 사용되었습니다.
    6. 주요기능
        1. 문서 유형 분류
            - DiT로 16개 카테고리 분류(letter, form, email, handwritten, advertisement, scientific report, scientific publication, file folder, news aricle, budget, invoice, presentation, questionnaire, resume, memo)
        2. OCR 및 구조화 정보 추출
            - PaddleOCR을 통한 한국어 텍스트 추출 및 바운딩 박스 검출
            - Donut/LayoutLMv3 를 활용한 영수증(상호명, 날짜, 시간, 금액 등)/문서 정보(제목,날짜,핵심내용 등) 추출
        3. 문서 요약 및 키워드 추출
            - KoBART 모델을 통한 한국어 텍스트 자동 요약
            - 불용어 제거를 통한 키워드 추출
        4. 웹 UI
            - 이미지 업로드 및 정보 추출
            - SQLModel 을 통해 SQLite 에 문서 메타데이터 저장 및 검색
            - 문서 이미지 상세 보기 및 다운로드
    7. 실행
        - 아래와 같이 명령어를 실행하여 의존 패키지를 설치하고 미니 프로젝트 어플리케이션을 실행합니다.
        - 실행하면 localhost:8501 로 접속이 가능하며 폴더 내 이미지들 중 하나를 선택해 업로드하면 문서 아카이빙이 시작됩니다.
        - 그 뒤에 하단의 저장 버튼을 누르면 DB에 데이터가 저장되어 문서 목록 탭에서 확인할 수 있습니다.
        - 그 뒤 문서 검색 탭을 클릭하고, 검색어를 입력한 뒤 검색 버튼을 클릭합니다. 
        - 벡터 유사도 검색은 검색어의 의미와 유사한 문서들을 찾아주고, 키워드 검색은 키워드가 포함된 문서를 찾아줍니다.
    8. 실습 과제 1: 이미지 전처리
        - OCR 인식은 이미지의 품질에 크게 좌우됩니다. 현재는 원본 이미지를 그대로 OCR 모델에 입력하여 인식률이 낮고 그로 인해 요약과 키워드 추출의 품질까지 함께 떨어지고 있습니다. 다양한 이미지 전처리 기법을 통해 OCR 정확도를 향상시켜보세요.
        - 요구사항
            - 이진화를 통한 텍스트와 배경 분리
            - 노이즈 제거 및 선명도 향상
            - 기울어진 문서 자동 보정
            - 전처리 전후 비교 옵션을 제공하고 비교 결과 시각화
        - 구현 Tip
            - 이미지 전처리: OpenCV를 활용하여 노이즈 제거, 대비 개선, 이진화, 모폴로지 연산, 기울기 보정 등 다양한 이미지 전처리 기법들을 적용해보세요.
            - UI 구현: Streamlit 으로 비교 결과에 대해 시각화하는 UI를 구현해보세요.
    9. 실습 과제 2: 형태소 분석
        - 단순히 불용어 제거를 통해 키워드를 추출하는 방식을 한국어 형태소 분석기를 활용한 키워드 추출 방식으로 바꿔 추출의 성능을 개선해보세요.
        - 요구사항
            - 한국어 형태소 분석기(Konlpy) 통합
            - 명사, 고유명사 위주의 키워드 추출
            - 복합 명사 및 신조어 처리
            - TF-IDF 기반 키워드 중요도 계산
        - 구현 Tip
            - 형태소 분석: Konlpy 의 Komoran, Okt 등을 활용하여 형태소를 분석하고 처리해보세요.
            - 신조어 처리: 신조어 처리를 위한 사용자 정의 키워드 데이터를 구축해보세요.
            - 중요도 계산: scikit-learn의 TF-IDF 를 활용해보세요. 문서의 유형을 세분화하고 저장된 문서들을 유형 별로 가져와 코퍼스(문서 집합)를 구성하여 TF-IDF를 계산하거나 문서가 부족한 경우 단어빈도(TF)로 키워드 중요도를 계산해보세요.
    10. 실습 과제 3: 사진 구분 및 메타데이터 검색
        - 현재는 문서가 아닌 일반 사진들은 구분하지 못하고 있습니다. 일반 사진을 구분하고 메타 데이터를 추추랗여 사진 이미지 또한 검색이 가능하도록 구현해보세요.
        - 요구사항
            - EXIF 데이터로 사진 구분 및 데이터 추출
            - 사진 내용 인식을 위한 객체 탐지
            - 사진 메타 데이터 기반 검색 기능
            - 사진에 위치 정보가 존재하는 경우 지도와 마커 표시
        - 구현 Tip
            - EXIF 처리: Pillow와 piexif 라이브러리를 활용해보세요.
            - 객체 탐지: 이미 많은 모델을 로드하고 있으므로 경량형 객체 탐지 모델을 활용하고 양자화를 지원하는 모델에 양자화를 설정하거나 동일한 모델 중 더 가벼운 모델로 교체 또는 Donut 등 우선순위가 낮은 모델은 제거해보세요.
            - 주소 변환: GPS 좌표를 주소로 변환하기 위한 지도 Open API를 활용해보세요.
            - 지도 표시: Streamlit 으로 지도 UI를 구현해보세요.