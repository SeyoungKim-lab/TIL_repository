# 헷갈릴만한거
raise_exeption = True
- article_list
전체글조회: ArticleSerializer(article, many=True)
단일글생성: ArticleSerializer(data=request.data)
- article_detail
단일글조회: ArticleSerializer(article)
단일글수정: ArticleSerializer(article,data=request.data)

- 댓글생성요청
    - urls.py : path("article/<article_pk>/comment/", views.comment),

    - serializers.py:
    CommentSerializer => read_only_fields = ("article",)

    ArticleSerializer 안에
    CommentSerializerForArticle 클래스 만들어서
    comment_set 을 시리얼라이저하고, many=Truem read_only=True

    - views.py
    CommentSerializer(data=request.data)
    하고 유효성검사안에서 save할때 article=article
    끝




